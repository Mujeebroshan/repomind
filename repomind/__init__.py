"""RepoMind — AI-powered deep codebase understanding agent.

RepoMind reads every file in a repository, builds hierarchical understanding
(file → directory → component → project), maps dependency graphs, and uses
that deep context to answer questions, suggest improvements, and generate code.

Uses Ollama for local/free bulk processing and free cloud APIs (Gemini, Groq,
OpenRouter) for complex reasoning tasks.
"""

__version__ = "0.2.0"
